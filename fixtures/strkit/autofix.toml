[build]
command = ["gcc", "-std=c99", "-Wall", "-Wextra", "-Werror", "-Isrc", "-Itests",
           "-o", "test_runner", "src/strkit.c", "tests/test_strkit.c"]
timeout = 120

[test]
command = ["./test_runner"]
format = "minitest"
timeout = 20

[sandbox]
protected = ["tests/**", "autofix.toml"]
